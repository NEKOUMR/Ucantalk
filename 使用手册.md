# 📘 Ucantalk 使用手册

**作者:** NEKO_UMR
**版本:** C# 重构版 (基于 WinUI 3 / .NET 8)
**适用系统:** Windows 10 (17763) 及以上，支持 x86 / x64 / ARM64

---

## 📢 交流与反馈

- QQ群号：**1040186268**（Bug 反馈 / 使用问题请加群）
- 遇到问题请优先查看本手册底部的 **[常见问题汇总]**，实在搞不懂再来问群主。
- 可以把本手册丢给 AI，让 AI 帮你解决问题。

---

## 📦 第一章：安装与初始化

与旧版（Python版）最大的不同是：**新版已完全不需要 Python 环境，也不需要手动安装插件。** 所有功能已内置，开箱即用。

### 1. 安装软件

根据下载包类型，二选一：

**方式A：安装包（推荐）**
- 运行 `Ucantalk_Setup.exe`，按提示完成安装即可。
- 程序会自动安装 .NET 8 运行库和 VC++ 依赖。
- 安装完成后，从开始菜单或桌面快捷方式启动。

**方式B：MSIX 包**
- 双击 `.msix` 文件，点击"安装"。
- 如提示证书问题，先双击 `.cer` 文件安装证书，再安装 MSIX。

> ⚠️ **建议：** 右键软件图标 -> 属性 -> 兼容性 -> 勾选 "以管理员身份运行此程序"（防止在游戏全屏时全局热键失效）。

---

### 2. 安装虚拟声卡（仅首次，VRChat 用户必须做）

新版同样需要 VB-Audio 虚拟声卡来将声音传入 VRChat 麦克风。

1. 在软件下载包内找到 `VBCABLE_Driver_Pack45`（或类似名称）文件夹。
2. 右键 `VBCABLE_Setup_x64.exe` -> **以管理员身份运行**。
3. 点击 "Install Driver"。
4. ⚠️ **安装完成后，必须重启电脑！**

---

### 3. 检查 FFmpeg（已内置）

软件目录下的 `tools/ffmpeg/` 文件夹中已自带 `ffmpeg.exe`。

> ⚠️ 请勿删除或移动该文件，否则**音频播放器**和**语音合成**将无法正常工作。

---

### 4. 语音识别模型（已内置 Sherpa 离线模型）

新版已在 `tools/sherpa-default/` 内置了一个高质量的中文离线语音识别模型，**无需额外下载**，首次使用语音识别时直接选择 "Sherpa-ONNX（默认模型）" 即可。

如果你需要更换 Vosk 模型，请参考第四章。

---

## 🎛️ 第二章：音频设置（关键步骤）

新版内置 "双通道音频路由"，能自动把声音同时发给**你自己**和 **VRChat**，无需 OBS 中转。

### 2.1 软件内设置

打开软件，点击左侧导航栏的 **⚙️ 设置**，找到 **🔊 音频路由** 区域：

| 设置项 | 操作 | 作用 |
|--------|------|------|
| 🎧 **监听设备**（给自己听） | 选择你的物理耳机/音响，如 `Speakers (Realtek Audio)` | 你能实时听到 AI 说话 |
| 📡 **VRC 设备**（发给麦克风） | 选择 `CABLE Input (VB-Audio Virtual Cable)` | 将声音发送给游戏 |

> ❓ 找不到设备？点击右侧的 🔄 **刷新** 按钮。还是没有 CABLE Input？说明第一章的驱动没装好或没重启。

---

### 2.2 VRChat 游戏内设置

进入 VRChat 菜单 -> **Audio** -> **Microphone**，将输入设备选择为：

```
CABLE Output (VB-Audio Virtual Cable)
```

---

## 🧩 第三章：界面总览与核心功能

软件左侧为导航栏，共 8 个功能区域：

| 导航项 | 功能说明 |
|--------|----------|
| 🏠 **主页** | 文字输入与发送，语音历史记录 |
| ⚙️ **设置** | 外观、音频路由、热键、代理等基础设置 |
| 🎤 **语音引擎** | TTS 引擎选择与配置，角色档案管理 |
| 🧩 **插件** | 扩展功能入口 |
| 🌍 **翻译设置** | 翻译引擎与目标语言配置 |
| 🎙️ **语音识别** | 语音转文字引擎与触发模式配置 |
| 🎵 **音频播放器** | 音乐/音效播放，支持双路输出 |
| 📋 **日志** | 运行日志查看，用于排查问题 |

---

### 1. 🏠 主页：文字发送

**基本操作：**
1. 在文字输入框（"输入要说的话"）中输入文本。
2. 点击 **发送** 按钮，或按配置的 **发送热键**。
3. 软件将合成语音并同时播放给你和 VRChat。

**主页功能开关：**
- **自动发送**：语音识别完成后自动触发发送，无需手动点击。
- **最近识别记录**：开启后，语音识别的历史记录会显示在主页下方，可点击重发。

---

### 2. ⚙️ 设置

#### 外观设置
| 项目 | 说明 |
|------|------|
| 主题 | 跟随系统 / 浅色 / 深色 |
| 强调色 | 自定义 UI 主色调（默认 `#8A8A8A`） |
| 背景图片 | 可设置自定义背景图 |
| 背景模糊 | 背景虚化程度（0-100） |

#### 热键设置
| 热键 | 功能 |
|------|------|
| **全局唤醒热键** | 将窗口置顶/激活 |
| **语音识别热键** | 触发/停止语音识别 |
| **发送热键** | 发送当前文本框内容 |

> 点击热键输入框后，按下想要的快捷键组合即可完成设置。

#### 其他设置
- **代理（Proxy）：** 如需使用代理访问 API，填写 `http://127.0.0.1:7890` 格式的地址。

---

### 3. 🎤 语音引擎（TTS 配置）

点击左侧 **🎤 语音引擎** 进入 TTS 配置页。

#### 支持的 TTS 引擎

**① Edge TTS（微软语音，推荐新手）**
- 无需任何配置，开箱即用。
- 多语言、多音色支持。
- 在 **音色** 下拉框中选择声音（如 `zh-CN-XiaoxiaoNeural`）。
- 可调节 **语速** 和 **音调**。

**② GPT-SoVITS（本地克隆音色，进阶）**
- 需先启动 GPT-SoVITS 后台服务（见第五章）。
- 配置项：
  - **API 地址：** 默认 `http://127.0.0.1:9880`
  - **输入语言** / **参考语言**
  - **播放速度**
  - **去除标点：** ✅ 使用 GPT-SoVITS 时建议开启，可解决说话"卡顿"或"长停顿"问题

**③ FishAudio（云端 API）**
- 需要填写 FishAudio 的 **API Key** 和 **参考音色 ID**。

---

#### 角色档案管理

每个"角色档案"保存一套完整的 GPT-SoVITS 配置（模型路径、参考音频等）。

| 操作 | 说明 |
|------|------|
| **新建** | 创建新档案（输入名称） |
| **重命名** | 修改当前档案名 |
| **删除** | 删除当前档案 |
| **切换** | 点击档案列表即可切换 |

每个档案包含：
- GPT 模型路径（`.ckpt` 文件）
- SoVITS 模型路径（`.pth` 文件）
- 参考音频路径（`.wav` / `.mp3`，3-10 秒为佳）
- 参考文本（参考音频中的台词）

---

### 4. 🌍 翻译设置

#### 翻译引擎

| 引擎 | 说明 |
|------|------|
| **通用 AI 大模型（推荐）** | 支持智谱、DeepSeek、通义千问等兼容 OpenAI 格式的 API |
| **Google 翻译** | 免费，无需 Key，但网络需能访问 Google |
| **DeepL** | 高质量翻译，需 DeepL API Key |

#### 目标语言
可配置最多 **3 个目标语言**，翻译结果以 `|` 分隔显示：

```
你好世界 | Hello World | こんにちは世界
```

- **主翻译语言：** 设置后，TTS 将朗读翻译后的内容（例如：输入中文，说出日文）。

---

#### 📘 智谱 AI（GLM-4-Flash）API 获取指南（免费推荐）

**第一步：注册**
1. 访问 [智谱 AI 官网](https://www.bigmodel.cn)，注册账号。
2. 新用户通常赠送免费额度。

**第二步：获取 API Key**
1. 登录后，鼠标移到右上角头像 -> 点击 **"API密钥"**。
2. 点击 **"添加新的 API Key"**。
3. 复制生成的 Key（格式类似 `2348382.sakjdhKJH...`）。

**第三步：配置到软件**
1. 打开 **🌍 翻译设置**。
2. 引擎选择 **"通用 AI 大模型"**。
3. API 地址填写：`https://open.bigmodel.cn/api/paas/v4/`
4. 模型名称填写：`glm-4-flash`
5. 粘贴 API Key。
6. 点击保存。

> 💡 **为什么推荐智谱？**
> - `glm-4-flash` 模型目前**完全免费**。
> - 国内服务器，无需梯子，速度快。
> - 对中文网络用语、二次元梗理解更准确。

---

### 5. 🎙️ 语音识别（语音转文字）

点击左侧 **🎙️ 语音识别** 进行配置。

#### 支持的识别引擎

| 引擎 | 特点 |
|------|------|
| **Windows** | 系统内置，无需配置，中文识别依赖系统语言包 |
| **Vosk** | 本地离线，精度高，需下载模型 |
| **Sherpa-ONNX** | 本地离线，精度最高，已内置中文模型，支持 GPU 加速 |

#### 触发模式

| 模式 | 操作方式 |
|------|----------|
| **Toggle（切换）** | 单击热键/按钮开始识别，再次单击停止 |
| **PTT（长按说话）** | 按住热键/按钮说话，松开即发送 |
| **连续识别** | 软件自动检测停顿并发送，彻底解放双手 |

#### Sherpa-ONNX 配置

| 项目 | 说明 |
|------|------|
| 模型路径 | 留空则使用内置默认中文模型 |
| 运算后端 | CPU（默认）/ CUDA（NVIDIA GPU）/ DML（DirectML） |
| 线程数 | 1-16，根据 CPU 核心数调整 |
| 解码方式 | greedy_search（快）/ modified_beam_search（精） |

#### 语音识别工作流

1. 在 **🎙️ 语音识别** 页完成引擎配置。
2. 回到主页，点击 **🎤 语音输入** 按钮（或按 **语音识别热键**）。
3. 说话，识别结果自动填入文字输入框。
4. 开启 **自动发送** 后，识别完成即自动合成播放，无需手动点击。

---

### 6. 🎵 音频播放器

支持双路输出：你自己听音乐的同时，VRChat 里的朋友也能同步听到。

- **支持格式：** MP3、FLAC、WAV 等（内置 FFmpeg，格式通吃）
- **用途：** 播放 BGM、角色语音包、整活音效等
- 音频路由自动读取 **设置** 页面中配置的音频设备

---

### 7. 📱 手机输入功能

不想切出游戏打字？用手机扫码控制！

**操作步骤：**
1. 在主页找到二维码显示区域。
2. 用手机扫描二维码（确保手机和电脑在**同一个 Wi-Fi** 下）。
3. 在手机浏览器中打开控制页面，直接在手机上输入文字并发送。

> ⚠️ 手机必须与电脑连接同一局域网（同一个 Wi-Fi），不能用 4G/5G 流量，也不能是路由器的"访客网络"。

---

## 🔧 第四章：Vosk 离线语音模型配置（可选）

如果你需要使用 **Vosk** 引擎（而不是内置的 Sherpa-ONNX），需要手动下载模型。

### 下载 Vosk 模型
推荐中文小模型：`vosk-model-small-cn-0.22`
下载地址：[https://alphacephei.com/vosk/models](https://alphacephei.com/vosk/models)

### 配置步骤
1. 解压下载的模型压缩包，得到一个包含 `model.conf` 的文件夹。
2. 打开软件 **🎙️ 语音识别** 页面。
3. 识别引擎选择 **Vosk**。
4. 点击 **Vosk 模型路径** 右侧的 📂 图标，选中包含 `model.conf` 的文件夹。

> ⚠️ **路径注意：** 若路径包含中文或空格，Vosk 可能加载失败。建议将模型放在全英文路径下（如 `C:\vosk-models\vosk-model-small-cn-0.22`）。

---

## 🔧 第五章：GPT-SoVITS 后台配置（进阶）

如果你想使用**克隆音色**功能（GPT-SoVITS 本地模式），必须先启动 GPT-SoVITS 后台服务。

### 1. 下载 GPT-SoVITS 整合包

- GitHub：[https://github.com/RVC-Boss/GPT-SoVITS/releases](https://github.com/RVC-Boss/GPT-SoVITS/releases)
- 国内直链1：ModelScope 镜像（搜索 GPT-SoVITS）
- 国内直链2：HuggingFace 镜像（hf-mirror.com）

---

### 2. 启动 API 服务（不是 WebUI）

> ⚠️ **核心原则：** 软件连接的是 `9880` 端口（API），不是网页版的 `9872` 端口（WebUI）。

1. 打开 GPT-SoVITS 整合包文件夹。
2. 找到 `go-api.bat`、`api.bat` 或 `启动API.bat`，**双击运行**。
3. 如果找不到这类文件，自己新建 `API.bat`，写入以下内容（必须放在 GPT 根目录）：

```bat
@echo off
runtime\python.exe api_v2.py
pause
```

4. ✅ **成功标志：** 黑色窗口显示 `Uvicorn running on http://0.0.0.0:9880`

---

### 3. 在软件中配置 GPT-SoVITS

打开 **🎤 语音引擎** 页面，选择引擎为 **GPT-SoVITS**，然后填入（或在角色档案中配置）：

| 配置项 | 说明 |
|--------|------|
| API 地址 | 默认 `http://127.0.0.1:9880` |
| GPT 模型路径 | `.ckpt` 文件路径 |
| SoVITS 模型路径 | `.pth` 文件路径 |
| 参考音频 | `.wav` 或 `.mp3`，3-10 秒为佳 |
| 参考文本 | 参考音频中说的台词 |
| 去除标点 | ✅ 建议开启，防止卡顿 |

---

## ❓ 第六章：常见问题汇总（FAQ）

---

**Q1：软件提示 "API 连接失败" 或 "Connection Error"**

- **原因 1：** GPT-SoVITS 后台没开，或者开成了网页版（WebUI）。
  → 运行 `api.bat`，确保黑框显示端口是 `9880`。
- **原因 2：** 端口号填错。
  → 检查软件 API 地址是否为 `http://127.0.0.1:9880`。
- **原因 3：** 开了加速器/全局代理。
  → 在软件设置的代理栏填入你的代理地址，或临时关闭系统代理。

---

**Q2：GPT-SoVITS 说话总是卡顿或中间长停顿**

→ 在 **🎤 语音引擎** 页面，开启 **"去除标点"** 开关。这是 GPT-SoVITS 模型对标点符号过敏导致的，过滤后可完美解决。

---

**Q3：GPT-SoVITS 一合成就没声音或闪退**

- **原因：** 显卡较新，而整合包内置 CUDA 版本较老，兼容性冲突。
- **解决：** 强制 CPU 计算。
  1. 打开 GPT-SoVITS 目录 -> `GPT_SoVITS/configs/tts_infer.yaml`。
  2. 用记事本打开，找到 `device: "cuda"`，改为 `device: "cpu"`。
  3. 保存后重启 API。

---

**Q4：软件里找不到 CABLE Input 设备**

→ 务必**重启电脑**。若重启后仍无，去 `VBCABLE_Driver_Pack45` 文件夹重新以**管理员身份**运行安装程序。

---

**Q5：弹窗提示 "模型加载失败: Failed to create a model"**

- **原因：** 语音识别引擎设置为 Vosk，但指定路径中找不到有效模型。
- **解决：**
  1. 打开 **🎙️ 语音识别** 页面。
  2. 切换引擎为 **Sherpa-ONNX（内置模型）** 或 **Windows**。
  3. 若坚持使用 Vosk，点击 📂 重新选择包含 `model.conf` 的文件夹。
  4. 检查路径是否包含中文或空格，建议改为全英文路径。

---

**Q6：翻译没有生效 / 翻译显示 "NoKey"**

→ 在 **🌍 翻译设置** 页面，确认已填入有效的 API Key。推荐使用智谱 AI（`glm-4-flash` 免费），配置方法见第三章。

---

**Q7：热键在游戏全屏时失效**

→ 右键软件图标 -> 属性 -> 兼容性 -> 勾选 **"以管理员身份运行此程序"**，重新启动软件。

---

**Q8：手机扫码后打不开网页，提示"无法连接"**

- **原因 1（最常见）：** 电脑网络设置为【公用网络】，防火墙拦截。
  → 点击右下角网络图标 -> 找到当前网络 -> 属性 -> 将"网络配置文件类型"改为 **【专用】**。
- **原因 2：** 防火墙拦截了软件端口。
  → 搜索"允许应用通过 Windows 防火墙"，确保 `Ucantalk.exe` 的【专用】和【公用】两个勾都打上。
- **原因 3：** 手机未连接同一 Wi-Fi。
  → 确保手机和电脑在同一个局域网，不要用 4G/5G 或访客网络。

---

**Q9：VB-Cable 采样率报错（音频输出异常）**

如果日志中出现采样率相关错误：
1. 打开 Windows 系统设置 -> 系统 -> 声音 -> **更多声音设置**（声音控制面板）。
2. 在"播放"选项卡中，找到 **CABLE Input**，右键 -> **属性** -> **高级**。
3. 将"默认格式"改为 **2通道，16位，44100 Hz**，点击确定。
4. 重启软件。

---

**Q10：语音识别 Sherpa-ONNX 引擎运行缓慢**

- 若 CPU 核心数较多，可在 **🎙️ 语音识别** 页面增大 **线程数**（如设为 4）。
- 若有 NVIDIA 显卡，将 **运算后端** 切换为 **CUDA**，可大幅提升速度。

---

**Q11：GPT-SoVITS 占用太大，VRChat 很卡**

→ 同 Q3，将 GPT-SoVITS 的 `tts_infer.yaml` 中 `device` 改为 `cpu`，使用 CPU 进行计算（性能下降但 GPU 负担小）。

---

**Q12：软件没有声音，但状态显示"发送成功"**

可能原因：
1. **音频路由未配置：** 检查 **⚙️ 设置** -> **音频路由**，确认监听设备和 VRC 设备已正确选择。
2. **VRC 麦克风未切换：** 确认 VRChat 内 Microphone 设置已改为 `CABLE Output`。
3. **音量为 0：** 检查软件内音量设置是否为 100%。

---

## 📁 附录：配置文件位置

软件运行时的配置文件自动保存在：

```
C:\Users\<你的用户名>\AppData\Roaming\Ucantalk\config.json
```

日志文件位置：
```
C:\Users\<你的用户名>\AppData\Roaming\Ucantalk\logs\
```

> 如果遇到严重问题，可以尝试删除 `config.json` 重置所有设置为默认值。

---

*本文档适用于 Ucantalk C# 重构版（WinUI 3 / .NET 8），旧版 Python 版手册不再适用。*
